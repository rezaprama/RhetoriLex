name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Reject tracked restricted sources
        run: python .github/scripts/check_tracked_files.py

      - name: Build deterministic data
        run: python scripts/build_data.py

      - name: Validate canonical data
        run: python scripts/validate_data.py

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

      - name: Validate bundled skill
        shell: bash
        run: |
          validator="$HOME/.codex/skills/.system/skill-creator/scripts/quick_validate.py"
          if [[ -f "$validator" ]]; then
            python "$validator" skills/rhetorilex
          else
            python .github/scripts/validate_skill.py skills/rhetorilex
          fi

      - name: Build and verify plugin package
        run: |
          python scripts/package_plugin.py --output dist/rhetorilex-plugin.zip
          python scripts/package_plugin.py --verify dist/rhetorilex-plugin.zip

      - name: Verify reproducible rebuilds
        run: python .github/scripts/check_determinism.py
